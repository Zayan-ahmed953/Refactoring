main = rule {
  environment_is_prod == "true" and
  prod_tfvars_exists == "true" and
  prod_tfvars_env_ok == "true" and
  prod_tfvars_region_ok == "true" and
  prod_tfvars_team_present == "true" and
  prod_tfvars_org_present == "true" and
  prod_backend_uses_s3 == "true"
}
