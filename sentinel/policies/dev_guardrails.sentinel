main = rule {
  backend_uses_s3 == "true" and
  ((s3_encryption_enabled else "true") == "true") and
  ((s3_versioning_enabled else "true") == "true") and
  ((s3_public_access_blocked else "true") == "true")
}