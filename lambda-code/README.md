# Lambda Code Layout

Store one folder per Lambda function in this directory:

```text
lambda-code/
  app-function/
    <source files>
```

This keeps function source separate from Terraform and promotion configs.
